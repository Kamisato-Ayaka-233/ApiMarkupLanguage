export enum Symbol {
  greaterThan = '>',
  lessThan = '<',
  equalTo = '=',
  openBrace = '{',
  closeBrace = '}',
  openSquareBracket = '[',
  closeSquareBracket = ']',
  openBracket = '(',
  closeBracket = ')',
  colon = ':',
  doubleQuotation = '"',
  singleQuotation = "'",
  comma = ',',
  commet = '#',
  dot = '.',
  asterisk = '*',
  minus = '-',
}